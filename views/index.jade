extends ./layout
block content
  h1 nimei
  if (authenticated)
    p Welcon back, #{me.first}
    a(href="/logout") Logout
  else
    p Welcome new visitor;
    ul
      li: a(href="/login") Login
      li: a(href="/signup") Signup