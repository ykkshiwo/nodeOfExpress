{{#each objArr}}
    <!--@key/@index 默认从0开始
    @first 判断是不是第一行
    还可以判断@last 是不是最后一行
    -->
    {{!-- {{@key}} --}}
    {{!-- {{@index}} --}}

    {{@first}} --  {{id}} -- {{weight}}<br/>
 {{/each}}